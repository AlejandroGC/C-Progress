% Alejandro Daniel Gonzalez Carrillo A01570396 

gb = input("Capacidad de disco duro (en gigabytes): ");
mbytes = gb * 1024
kbytes = mbytes * 1024;
bytes = kbytes * 1024

disp("La capacidad en gigabytes es "+ gb);
disp("La capacidad en megabytes es "+ mbytes);
disp("La capacidad en kilobytes es "+ kbytes);
disp("La capacidad en bytes es "+ bytes);
